import styled from "styled-components";

const Footer = () => {
  return <Container>개발자 정보</Container>;
};

const Container = styled.div`
  display: flex;
  justify-content: end;
  height: 100px;
`;
export default Footer;
